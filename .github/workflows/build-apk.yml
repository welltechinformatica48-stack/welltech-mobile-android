name: Build Welltech Mobile Agent APK

on:
  push:
    branches: [ "main", "dev/agent" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Gradle 8.11.1
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      - name: Build debug APK
        run: gradle :app:assembleDebug --stacktrace

      - name: Rename APK
        run: |
          mkdir -p output
          cp app/build/outputs/apk/debug/app-debug.apk output/Welltech-Mobile-Agent-0.2.2-state-sync.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Welltech-Mobile-Agent-0.2.2-state-sync
          path: output/Welltech-Mobile-Agent-0.2.2-state-sync.apk
          if-no-files-found: error
